# Planned Features

This directory holds placeholder modules for CLIverse features that are visible in the launcher but not implemented yet.

Implemented today:

- `sysmon`
- `disk`

Planned modules are grouped by launcher category so each one can be developed incrementally without reshaping the repository later.
