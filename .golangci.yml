version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam
    - prealloc
    - bodyclose

run:
  timeout: 5m
  modules-download-mode: readonly
